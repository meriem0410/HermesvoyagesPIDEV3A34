{% extends 'base_back.html.twig' %}

{% block title %}Edit Transport{% endblock %}

{% block content %}
<div class="col-lg-6 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Edit Transport</h4>
            {{ include('transport/_form.html.twig', {'button_label': 'Update'}) }}
            <a href="{{ path('app_transport_index') }}" class="btn btn-warning btn-sm">Back to List</a>
        </div>
    </div>
</div>

{{ include('transport/_delete_form.html.twig') }}
{% endblock %}
