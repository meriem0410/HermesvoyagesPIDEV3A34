{% extends 'base_back.html.twig' %}

{% block title %}New Tickets{% endblock %}

{% block content %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Create new Tickets</h4>
                    {{ include('tickets/_form.html.twig') }}
                    <a href="{{ path('app_tickets_index') }}" class="btn btn-warning btn-sm">Back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
