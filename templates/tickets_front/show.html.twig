{% extends 'base.html.twig' %}

{% block title %}Tickets{% endblock %}

{% block body %}
    <h1>Tickets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket.id }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ ticket.destination }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ ticket.prix }}</td>
            </tr>
            <tr>
                <th>Depart</th>
                <td>{{ ticket.depart }}</td>
            </tr>
            <tr>
                <th>Arrive</th>
                <td>{{ ticket.arrive }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tickets_front_index') }}">back to list</a>

    <a href="{{ path('app_tickets_front_edit', {'id': ticket.id}) }}">edit</a>

    {{ include('tickets_front/_delete_form.html.twig') }}
{% endblock %}
